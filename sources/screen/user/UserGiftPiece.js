import { StyleSheet, Text, View } from "react-native";
import React from "react";

const UserGiftPiece = () => {
  return (
    <View style={styles.container}>
      <Text>UserGiftPiece</Text>
    </View>
  );
};

export default UserGiftPiece;

const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: "100%",
    backgroundColor: "#212529",
  },
});
